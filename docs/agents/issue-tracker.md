# Issue tracker

Issues and PRDs for this repository live in GitHub Issues at
`satyasaibhushan/Progress`.

Use the connected GitHub integration for issue reads and writes. Infer the
repository from the `origin` remote when working locally.
