# Domain documentation

This is a single-context repository.

Before changing domain behavior, read the root product documentation and any
relevant architectural decision records. If a root `CONTEXT.md` or files under
`docs/adr/` are added later, treat them as authoritative for domain terminology
and architectural decisions.

Use established product terms in issues, tests, and code. Surface conflicts
with documented decisions instead of silently overriding them.
